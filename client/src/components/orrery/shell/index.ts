/**
 * Orrery shell — layout primitives used by hero-tier pages.
 * Phase 1 ships MobileBottomSheet; Phase 1+ adds OrreryShell wrapper and
 * the PresentationBadge for observatory/clinical mode toggling.
 */
export { MobileBottomSheet, type SnapHeight } from "./MobileBottomSheet";
